import type { Life } from '@/lib/types'

/**
 * Life. Empty on purpose.
 *
 * This is his own account of himself, so it is written in Studio rather than
 * seeded here. Until it exists the section does not appear on the home page and
 * /life is not linked from anywhere.
 */
export const seedLife: Life | null = null

import type { Post } from '@/lib/types'

/**
 * Posts written here rather than on Medium.
 *
 * Empty by design — nothing is invented on his behalf. Write the first one at
 * /studio and it appears in the index beside the Medium articles.
 */
export const seedPosts: Post[] = []
